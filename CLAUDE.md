# CLAUDE.md

このファイルは ExecSandbox プロジェクトで作業する際、Claude Code が常に踏まえるべき
前提を示す。

## ExecSandboxとは

WASM実行ランタイムとWASMモジュールを1つの実行ファイル内に保持する、環境構築不要の
ポータブルなサンドボックス実行ツール。Go製、中核は `wazero`（Pure-Go WASM
ランタイム）。複数のExecSandboxインスタンスをメールボックス方式のメッセージング
で繋ぎ合わせてシステムを構成する。詳細は `docs/spec/execsandbox_spec_ja.md` を参照。

先行プロジェクト「ExecDB」（DBエンジンとデータ領域を単一バイナリに内包するRDBMS）
と対になる構想であり、構成パターン・開発の進め方の多くをそこから踏襲している。

## 参照すべきファイル

作業を始める前に、以下を確認すること。

- **`docs/spec/execsandbox_spec_ja.md`** — ExecSandboxの仕様書。用語、構成、
  サンドボックス間通信、外部接続、WASM ABI、ビルド、起動時CLI、ポリシー、
  制限事項、ライセンスを網羅する。
  **この仕様書は設計を固めきった上で確定させたものであり、ExecDBの仕様書とは
  性質が異なる（あちらは実装しながら育てるドラフトだった）。実装の都合で仕様を
  変えたくなった場合、勝手に書き換えず必ず提案して指示を仰ぐこと。**
  仕様の不備・矛盾・記述漏れを見つけた場合も同様に報告する。
  各項目には**その仕様を選んだ判断理由と、検討して退けた案**が併記されている。
  **実装中に「こうしたほうが良いのでは」と思った点は、まず該当箇所の理由を
  確認すること。** 既に検討して退けられている可能性が高い。仕様書にない新しい
  論点であれば、提案する価値がある。
- **`PLAN.md`** — 実装計画・進捗管理（開発フェーズ、現在地、保留事項）。
  リポジトリ直下に置く一級ドキュメント（Claude Code自身の作業ログという性質上、
  ここに留めている）。
  **セッションをまたぐたびに「現在地」を確認・更新すること。**
- **`.claude/rules/abi-compatibility.md`** — WASM ABIの互換性（一度公開したABIは
  壊せない。拡張の作法、値域の予約）
- **`.claude/rules/stamp.md`** — スタンプ方式（フッター構造、埋め込みと取り出し、
  ExecDBのPoCから引き継いだ知見と、引き継がない部分）
- **`.claude/rules/directory-structure.md`** — ディレクトリ構成（本体・ビルダー・
  配置の判断基準、SDKライブラリが別リポジトリである理由と依存の向き）
- **`.claude/rules/naming.md`** — 命名規則（ABI関数名・SDK関数名・CLIオプションの
  対応関係、短縮フラグ）
- **`.claude/rules/testing.md`** — テスト方針（開発環境の前提、検証用WASM
  モジュール、実装後の動作確認）
- **`.claude/rules/cli-output.md`** — CLI出力（ホスト側ログとゲストstderrの区別、
  ログのレート制限、装飾ライブラリを入れない方針）
- **`.claude/rules/binary-size.md`** — 実行ファイルサイズ（本体の増加がビルダーで
  6倍に増幅される構造）
- **`.claude/rules/distribution.md`** — 配布方法（本体とビルダーの二段リリース、
  生成物のライセンス表示義務、リポジトリのDescription/Topics）

これらのルールファイルは、実装中に得た細かい気づき・教訓を Claude Code 自身が
育てていくものである。新しく気づいたルール・過去に踏んだ落とし穴などがあれば、
該当するファイルに追記すること。どのファイルにも当てはまらない新しい種類の
気づきであれば、新しいルールファイルを作ってよい（`wazero`固有の落とし穴が
溜まってきたら `wazero-quirks.md` を作る、など）。

## 利用者向けドキュメント（`docs/`）

`docs/spec/` 以外に、利用者向けのドキュメントを `docs/` 配下に置く。
`.claude/rules/` が「Claude Codeが従うべき開発ルール」であるのに対し、
`docs/` は「ExecSandboxを使う人が読むもの」であり、役割が異なる。

構成の定義と、どこに何を書くかの判断基準は
`.claude/rules/directory-structure.md` の「`docs/` — 利用者向けドキュメント」を
参照すること。

現時点で存在するのは `docs/spec/` と `docs/usage/` のみ。`docs/examples/`・
`docs/tour/` は未作成であり、作成のタイミングは
`.claude/rules/directory-structure.md` に記載している。

**`docs/usage/` は実装前に仕様から起こしたものであり、コマンドの出力例は
未検証である。** 実装が進んで実際に動かせるようになったら、実測値へ差し替える
こと。また、仕様に変更があった場合はこちらも追随させること（`docs/spec/` だけ
直して `docs/usage/` が古いまま、という状態を作らない）。

## 開発の進め方

- 現在の開発フェーズは `PLAN.md` の「現在地」を確認すること。フェーズが進んだら
  更新すること。
- 実装後は `.claude/rules/testing.md` に従い、ビルド確認に加えて実際の動作確認を
  行うこと。判断に迷ったら実行する側に倒すこと。
- 依存ライブラリを追加する場合、ライセンスがMIT（本プロジェクトのライセンス）と
  衝突しないか確認すること（GPL系等は避ける。詳細は `.claude/rules/testing.md`
  の trivy チェックの節を参照）。また、依存の追加は
  `.claude/rules/binary-size.md` の観点でも一度立ち止まること。

## 権限・自動化について

`.claude/settings.json` により、`git push`/`pull`/`fetch`/`clone`、`curl`/`wget`、
パッケージインストール系コマンド（`*install*`, `go get`）は実行前に確認を求める
設定になっている。確認を求められた場合、無理に実行しようとせず、指示を仰ぐこと。

ビルドの自動フック（`make build` 等）は現時点では設定していない（`Makefile` の
中身が固まっていないため。詳細は `PLAN.md` の「保留事項」参照）。実装がある程度
進んだら、フック設定の要否を提案すること。

## ルール・スキルの提案

作業を進める中で、以下に気づいたら、都度こちらから提案すること（提案するだけで、
勝手に作成・適用はしない。判断はこちらが行う）。

- **新しいルールにした方がよさそうな知見**: 同じ種類の判断や落とし穴に複数回
  遭遇した、既存の `.claude/rules/` のどれにも当てはまらない、といった場合。
  どのファイルに追記すべきか、あるいは新規ファイルが必要かも合わせて提案する。
- **Skill化した方が効率的そうな作業**: 「同じ手順を3回目繰り返している」など、
  パターン化できそうな一連の作業に気づいた場合。まだ実装が薄い段階では無理に
  提案する必要はない。パターンが実際に繰り返されてから提案すること。

提案は気づいたタイミングで随時行ってよく、まとめて報告するために貯めておく必要は
ない。

## 言語について

ドキュメント・コミットメッセージ・コメントは日本語で書く。ExecDBは実装完了後に
英日併記化を行ったが、ExecSandboxでは当面日本語のみで進め、英語版の要否は
後で判断する（ファイル名の `_ja` サフィックスは、その将来の併記を見越したもの）。

コード内の識別子・ABI関数名・CLIオプション名は英語。

**CLIの出力（標準出力・標準エラー出力）はすべて英語で書く。** これは上記の
「ドキュメントは日本語」という方針の例外であり、以下が対象になる。

- ホスト側ログ（`execsandbox:` で始まる行）
- 起動時のエラーメッセージ
- `--help` / `--version` の出力

理由は `.claude/rules/cli-output.md` を参照。テストがこれらの文言を検証する
場合も、当然英語で書く。
